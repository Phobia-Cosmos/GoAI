# GoAI 完整系统结构与 Agent 协作设计

当前项目全量进度、数据可信边界和后续实施路线统一记录在：[GoAI 项目现状、目标与 Agent 实施路线图](docs/PROJECT_STATUS_AND_AGENT_PLAN.md)。本文件继续保留架构细节，若两者存在阶段描述差异，以路线图中的最新进度为准。

## 系统目标

系统的最终目标不是“根据历史报表写一段建议”，而是在指定比赛、指定规则版本和当前可见信息下，持续维护可信企业状态，生成多个可执行经营方案，在同一状态机中仿真其现金、库存、产能、交期、报表和风险，经人工确认后提交动作，并记录结果用于复盘和后续学习。

原始 VPD/OE Agent 和报表 Agent 都不是完整决策系统。VPD/OE 负责分析“经营结构是否健康”；报表 Agent 负责回答“到当前事件为止，企业真实状态和财务结果是什么”。真正的决策 Agent 还需要回答“下一阶段有哪些合法动作、多个季度方案中哪个更优、为什么、执行后发生了什么”。

## 精简后的项目视图

```text
goai/
├── src/goai_data/          # 当前统一数据、RulePack、状态和实验决策内核
├── scripts/                # 数据处理、实验和样例生成入口
├── tests/                  # 主项目自动测试
├── examples/               # 可复现输入、候选方案和完整数据样例
├── experiments/            # 实验说明；大体积输出通过软链接存储
├── origin data/            # 原始资料和两个旧 Agent，只读参考与迁移来源
├── SYSTEM_ARCHITECTURE.md  # 本文件
├── README_数据处理.md
├── README_PreAgent.md
└── README_Agent.md
```

新系统不再复制 VPD 和报表 Agent 的全部目录。迁移后的业务代码只需要四个逻辑组件，它们可以先共存于一个 Python 包，成熟后再拆服务：

| 组件 | 唯一职责 | 主要输入 | 主要输出 |
| --- | --- | --- | --- |
| 数据与规则层 | 来源登记、规则绑定、事件标准化、可见性和版本管理 | 原始 XLS、正式规则、平台日志 | `DatasetCatalog`、`RulePack`、`NormalizedEvent` |
| 报表状态内核 | 确定性重放、自动结算、财报和审计 | RulePack、初始状态、事件 | `GameState`、`Reports`、`Audit`、`Validation` |
| VPD 诊断器 | 对已确认状态和公共信息计算结构指标 | 状态、报表、订单和公共巡盘 | `MetricBundle`、风险与归因 |
| 决策编排器 | 生成候选、多季仿真、比较、确认和提交 | 可见信息、状态、规则、指标 | `DecisionCycle`、候选方案和事件草稿 |

LLM 不是第五个状态真值组件。它是可选接口，可用于自然语言转事件草稿、检索规则说明、解释指标和生成方案文字；确定性规则引擎负责计算与校验，人工确认负责高风险提交。

## 两个 Agent 如何协作

```text
平台日志 / 人工输入 / 历史 XLS
              |
              v
      数据与规则层：标准化、绑定、来源标记
              |
              v
      报表状态内核：事件重放和结算
              |
      +-------+----------------+
      |                        |
      v                        v
 GameState + Reports      Audit + Validation
      |
      v
 VPD 诊断器：PSS/EPSS、商分、风险和归因
      |
      v
 决策编排器：生成多个候选计划
      |
      v
 报表状态内核的沙盒分支：逐候选多季度重放
      |
      v
 规则/现金/库存/产能/交期/报表/评分比较
      |
      v
 人工确认 -> 提交标准事件 -> 新状态 -> 下一轮
```

报表状态内核应是在线状态的唯一真值来源。VPD/OE 当前的季度重建器只用于迁移历史数据；在线系统不得让 VPD 和报表 Agent 分别维护两份状态，否则相同动作会得到不同现金、库存和报表。VPD 输出必须带 `input_state_id`，决策候选也必须带 `base_state_id`；若当前状态已变化，旧候选自动失效。

## 核心数据契约

| 契约 | 必要标识 | 作用 |
| --- | --- | --- |
| `DatasetManifest` | `dataset_id`、源文件哈希、数据类别 | 防止重复、混场和把模拟数据当事实 |
| `RuleBinding` | `match_id`、`rule_pack_id`、绑定状态、证据 | 决定一场数据能否使用某套规则 |
| `RulePack` | `rule_pack_id`、`rule_version`、字段证据和缺口 | 所有引擎唯一规则来源 |
| `InformationSet` | `available_at_phase`、可见主体、记录时间 | 防止使用未来或对手不可见信息 |
| `NormalizedEvent` | `event_id`、期间、阶段、类型、payload、来源 | 驱动状态机的可重放业务凭证 |
| `GameState` | `state_id`、`previous_state_id`、规则版本、期间 | 当前企业完整状态 |
| `Reports` | `report_id`、`input_state_id`、会计版本 | 综合费用表、利润表和资产负债表 |
| `MetricBundle` | `metric_bundle_id`、公式版本、输入快照 | VPD/OE、PSS/EPSS、商分和风险 |
| `DecisionCycle` | `decision_id`、信息集、候选、选中方案、结果 | 记录 Agent 每次为什么这样决策 |
| `AuditRecord` | 输入、输出、校验、人工确认和提交状态 | 完整追溯与实验复现 |

所有数据值至少具有以下来源等级之一：`observed` 表示源文件或平台直接观测，`derived` 表示由已声明公式确定性计算，`inferred` 表示根据不完整历史重建，`simulated` 表示用于结构演示或候选推演，`missing` 表示当前未知。只有 `observed + derived` 且规则绑定已确认的数据可以成为正式回放基准。

## 当前已有能力

| 部分 | 已有能力 | 当前限制 |
| --- | --- | --- |
| 数据层 | Excel/Docx/Markdown 解析、SQLite/CSV、来源清单、动作标准化 | 主管道当前未覆盖 AB 至 OP 全部中文簇，catalog 需要 v2 |
| RulePack | 710W 部分题面已结构化，规则缺口已记录 | 中文十簇只有观察指纹；GC 和报表硬编码规则均未绑定 |
| 报表 Agent | 事件协议、reducer、自动事件、三张报表、对账、会话和 UI | 规则不是共享真值，尚未用中文十簇做整场回放 |
| VPD/OE | PSS/EPSS、耦合、商分、生命周期权重、报告 | 存在错误规则绑定、季度推断和会计口径未审计问题 |
| Pre-Agent | BOM、生产周期、交付、应收、状态守恒和 Pareto 比较 | 依赖 710W 部分规则，只能实验运行 |
| 决策 Agent MVP | 动作枚举、基础校验、现金沙盒和候选排序 | 尚不是完整经营策略，只覆盖少量状态转移 |

## 后续需要实现

1. 建立 `dataset_catalog_v2`、`rule_catalog_v2` 和 `dataset_rule_binding_v2`，优先完整登记 AB、CA、CB、CD、CE、EA、EB、EC、EF、OP；GC、ZY/AG/ZZ 和 710W 分开保存。
2. 找到中文十簇的正式规则、初始配置、赛前全局订单池和阶段操作手册。找不到时将缺口保留为 `missing`，不能用 GC 或 710W 参数补齐。
3. 将报表 Agent 的 `NormalizedEvent`、`GameState`、`Reports` 提升为共享协议，并让所有字段携带规则版本、状态 ID、来源等级和可见阶段。
4. 把报表状态机改为加载共享 RulePack，完成一场中文历史企业的逐事件回放，对现金、年度报表、贷款、库存、产线和资格逐项对账。
5. 用同一状态快照重算 VPD/OE，审计 VPD 与 OE 的会计边界、费用分摊、PSS 粒度、耦合阈值和生命周期权重；整场留出验证通过前不重新训练通用权重。
6. 实现决策阶段模型和 `phase × action × precondition × automatic_settlement` 权限表，补齐订单选择、融资、采购、生产、交货和年末结算动作。
7. 实现候选生成器和多季度滚动规划。每个候选都必须在报表状态机的隔离分支中重放，并按最低现金、违约、权益、利润、产能利用、订单价值和风险进行多目标比较。
8. 实现全局订单与对手情景模块。真实全局订单池缺失时只能做已分配订单分析，不能训练广告选单策略；对手未知时使用多个可解释情景而不是伪造确定预测。
9. 建立人工确认、提交适配器、失败恢复和线上审计闭环。LLM 可以生成解释或事件草稿，但不能绕过 schema、规则校验和确认。
10. 最后再实现高保真多企业模拟器、自对弈或强化学习。没有正式规则和可验证状态机时，所谓自进化只会学习模拟假设。

## 完整样例的用途

`examples/complete_match_v1/` 给出一套结构完整但明确标记为模拟的五年比赛数据。它用于回答“未来需要收集的数据完整形态是什么”，也用于接口和校验测试；它不能进入历史训练集，不能证明中文十簇真实存在其中的订单、BOM、融资或评分规则。

寻找新资料时，应优先补齐样例中标为 `missing` 的部分：正式 RulePack、初始状态、阶段权限、全局订单池、广告与选单规则、自动结算顺序、贷款/税/破产/评分规则、每季完整快照和平台提交结果。只有资料能落到这些明确字段，并附带来源、比赛 ID、规则版本、导出时点和可见阶段，才算真正补齐数据。
